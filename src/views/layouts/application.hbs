<!DOCTYPE html>
<html lang="en">
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1" charset='utf-8' />

  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>

  <link rel="stylesheet" href="/static/css/main.css" type="text/css">

  <title>
    {{title}}
  </title>
</head>
<body>
  <div class="container">
    {{#if user}}
      <header>
        <nav>
          {{user.name}} (<strong>{{user.role}}</strong>) {{#if user.isAdmin}}| <a href="/users">Administer</a>{{/if}} | <a href="/logout">Log Out</a>
        </nav>
      </header>
    {{/if}}

    <main>
      <h1>{{title}}</h1>
      {{{body}}}
    </main>
  </div>
</body>
</html>
